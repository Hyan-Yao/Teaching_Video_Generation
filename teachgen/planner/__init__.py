"""Planner (Phase 1) — text only. Cheap, fast, and produces a reviewable LessonPlan.

- content_writer: topic -> objectives + per-segment spoken narration
- route:          narration -> per-segment modality + visual brief (the only outline)
"""
