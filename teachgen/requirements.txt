# ---------------------------------------------------------------------------
# teachgen requirements
# ---------------------------------------------------------------------------
# Core orchestration + the slide/concept_image/audio/compositor paths.
openai>=1.40            # single-key provider: chat, vision, TTS, whisper, images
pydantic>=2.5           # data contracts (schema.py) + structured outputs
moviepy>=1.0.3          # compositing + frame sampling (1.x and 2.x both supported)
pillow>=10.0            # PIL slide rasterizer + frame encoding
python-pptx>=0.6.23     # make_slide.py text parsing (imported by the slide renderer)

# ---------------------------------------------------------------------------
# System packages (install with your OS package manager, NOT pip)
# ---------------------------------------------------------------------------
#   ffmpeg          required — compositing, audio probing, frame extraction
#                   macOS:  brew install ffmpeg
#                   ubuntu: apt-get install ffmpeg
#
# LibreOffice/poppler are NOT required — slides rasterize via Pillow.

# ---------------------------------------------------------------------------
# For the `animation` renderer (code2video / Manim) ONLY
# ---------------------------------------------------------------------------
# Manim Community Edition + its native deps (cairo, pango); see code2video's own
# pins in ../code2video/requirements.txt. Without Manim installed, animation segments
# transparently fall back to concept images.
#   pip install -r ../code2video/requirements.txt      # includes manim and code2video deps
#   manim --version                             # verify (tested with Community v0.19.0)
